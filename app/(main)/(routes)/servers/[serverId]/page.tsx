import React from "react";

interface IProps {
  name?: string
}

const ServerPage: React.FC<IProps> = (props) => { 
  return (
    <div>
      Server Id Page
    </div>
  )
 }

 export default ServerPage;